{
  "branches": ["master"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/npm",
      {
        "npmPublish": false
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "npm run make"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["package.json", "package-lock.json", "CHANGELOG.md"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "out/make/zip/win32/x64/*.zip",
            "label": "CreditsTracker-${nextRelease.version}-win32-x64.zip"
          },
          {
            "path": "out/make/squirrel.windows/x64/*.exe",
            "label": "CreditsTracker-${nextRelease.version}-win32-x64-Setup.exe"
          },
          {
            "path": "out/make/squirrel.windows/x64/*.nupkg"
          },
          {
            "path": "out/make/squirrel.windows/x64/RELEASES"
          }
        ]
      }
    ]
  ]
}
